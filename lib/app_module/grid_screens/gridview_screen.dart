import 'package:flutter/material.dart';

class GridViewScreens extends StatelessWidget {
  const GridViewScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
